#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    printf("saisir un nombre :");
    scanf("%d",&n);
    if(n<=0)
        {
            printf("le nombre que vous avez saisi est un nombre negative");

    }

    return 0;
}
